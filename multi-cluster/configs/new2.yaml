apiVersion: fleet.cattle.io/v1alpha1
kind: ClusterGroup
metadata:
  name: new2
  namespace: allclusters
spec:
  selector:
    matchLabels:
      env: dev
      location: rtp
      role: master

---
apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: new2
  namespace: allclusters
spec:
  branch: master
  paths:
    - multi-cluster/configs/new2
  repo: https://github.com/girishcx/fleet-examples
  targets:
    - clusterGroup: new2
      name: new2

apiVersion: fleet.cattle.io/v1alpha1
kind: ClusterGroup
metadata:
  name: new1
  namespace: allclusters
spec:
  selector:
    matchLabels:
      env: dev
      location: rtp
      role: master

---
apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: new1
  namespace: allclusters
spec:
  targets:
    - clusterGroup: new1
      name: new1
  branch: master
  repo: https://github.com/girishcx/fleet-examples
  paths:
    - multi-cluster/configs/new1

apiVersion: fleet.cattle.io/v1alpha1
kind: ClusterGroup
metadata:
  name: test123
  namespace: allclusters
spec:
  selector:
    matchLabels:
      env: dev
      location: rtp
      role: master

---
apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: test123
  namespace: allclusters
spec:
  targets:
    - clusterGroup: test123
      name: test123
  branch: master
  repo: https://github.com/girishcx/fleet-examples
  paths:
    - multi-cluster/configs/test123
